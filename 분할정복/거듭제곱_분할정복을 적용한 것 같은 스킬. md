```
public static int pow(int base, int exponent) {
		int result = 1;
    


		while(exponent > 0) {
			if( (exponent & 1) == 1) result = result * base;
			exponent = exponent >> 1;
			base = base * base;
		}
		
		return result;
	}
```
